# `POST /auth/dev-login` — 개발용 발급구

이메일만 주면 세션 토큰을 내준다. **`ALLOW_DEV_LOGIN=true` 일 때만** 열리고,
아니면 404 를 돌려준다. 운영에서는 꺼져 있다.

카카오 로그인은 브라우저를 거쳐야 해서 자동 검증에 쓰기 어렵다. 헤드리스로
관리자 화면을 확인할 때 이 발급구나 직접 서명한 토큰을 쓴다.

구현: `server-py/app/main.py`
