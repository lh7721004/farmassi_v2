# 백로그

디스코드 **#수정방**(`stable fusion` 서버)에 올라오는 요청을 정리해 둔 곳이다.
스레드는 요청이 오가는 자리고, 이 문서는 **무엇을 만들지 확정된 내용**을 담는다.

- 원문: https://discord.com/channels/1458046783795236947/1541094943857320058
- 마지막 정리: 2026-08-24 (스레드 11개 / 요청 12건 기준)
- 사실 확인: 코드·DB 를 직접 조회해 적었다. 추측은 `조사` 에 반증까지 남긴다.

---

## 이 문서를 쓰는 방법

새 요청이 올라오면 아래 순서로 처리한다.

1. **스레드를 끝까지 읽는다.** 채널 본문에는 제목만 있고 실제 요구사항·이미지·링크는
   전부 스레드 안에 있다. 본문만 보면 껍데기만 보게 된다. (실제로 한 번 그랬다)
2. 해당 우선순위 절에 항목을 추가한다. **ID 는 다시 쓰지 않는다** — 끝난 항목도
   `## 완료` 로 옮길 뿐 번호를 재사용하지 않는다.
3. 조사해서 알아낸 것(이미 있는 기능인지, 외부 제약이 있는지)은 **`조사`** 에 적는다.
   같은 걸 두 번 파헤치지 않기 위한 것이다.
4. **제목은 디스코드 원문 그대로 쓴다.** `[신규] 주문 들어오면 알림 필요` 처럼
   말머리까지 옮긴다. 임의로 줄이거나 번호를 붙이면 어느 스레드 얘기인지
   서로 못 알아본다.
5. **반응 이모지를 확인한다.** 채널 본문 메시지에 달린 반응이 상태 표시로 쓰인다.
   ☑️ 는 처리됨, 🔄 는 진행 중. 스레드 본문만 읽으면 이미 끝난 일을 다시 파게 된다.
   (실제로 그럴 뻔했다 — 재현 안 되던 사진 버그에 이미 ☑️ 가 달려 있었다.)
6. 답이 필요한 건 **`미결`** 에 적고, 아래 [미결 질문](#미결-질문) 표에도 올린다.

### 항목 형식

```
### [말머리] 디스코드 스레드 제목 그대로
- 우선순위 / 상태(반응 이모지 포함) / 요청자 / 스레드 링크
- 요구  : 무엇을 만드는가 (요청자 표현 그대로 살릴 것)
- 조사  : 확인한 사실. 이미 있는 기능, 외부 제약, 반증된 추측
- 범위  : 손대야 하는 파일·계층, 작업 브랜치
- 완료조건 : 무엇이 되면 끝인가
- 미결  : 답이 있어야 진행되는 것
```

### 우선순위

요청자가 쓰는 말을 그대로 따른다: **긴급 / 우선 / 일반 / 기타**.
기한이 붙은 것(`일반(추석 이전 주까지)`)은 기한을 함께 적는다.

### 브랜치

`ehyun`(server/, DB, 배포) · `ui`(src/ 화면) → `develop` → `main`.
한 항목이 양쪽에 걸치면 **범위** 에 둘 다 적고 커밋을 나눈다.

---

## 긴급

### [신규] 주문 들어오면 알림 필요
- **우선순위** 긴급 · **상태** 🔄 진행 중(반응) · 방식 미정 · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541102592422187078
- **요구** 주문이 들어오면 어떤 방법이든 박지훈에게 알림. "일단 10분마다 확인"
- **조사**
  - 서버에 이미 웹푸시가 있다. `create-order` → `notify_farm_members()` 가
    `notifications` 행을 만들고 `push_subscriptions` 로 발송한다.
  - 안 오는 이유는 둘 중 하나다 — 박지훈이 그 농가의 `farm_members` 에 없거나,
    브라우저 푸시를 구독하지 않았다. **기능이 없는 게 아니라 대상이 아닌 것.**
  - 메일 경로도 이미 있다. `send_deposit_mail()` 이 구글 SMTP 로 나가므로
    주문 생성에 같은 걸 붙이는 게 가장 빠르다.
  - 스레드에서 김이현 "어느 메일로?", min9004 "아니면 디코 봇?" — 결론 없음.
- **범위** `server/src/functions/createOrder.ts`, `server/src/shared/mailer.ts` / `ehyun`
- **완료조건** 주문 생성 시 박지훈에게 실제로 도착한다. 중복 발송 없음.
- **미결** 알림 수단(메일 / 디스코드 봇 / 푸시 구독 붙이기), 메일이면 주소

---

## 우선

### [신규] 보내는 분, 입금자명 정보 추가
- **우선순위** 우선 · **상태** 대기 · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541106378452500551
- **대상** https://www.farmassi.kr/farm/jinyeongnongwon/checkout
- **요구**
  - 배송지 **위**에 "보내는 분" 폼 추가
  - **입금자명·전화번호 필수**, 이전에 입력한 적 있으면 자동 입력
  - 보내는 분 주소·이름은 선택
- **조사** 지금 `orders` 에 입금자명 컬럼이 없다. 입금 대사는 `deposit_code`(6자리)와
  `recipient_name` 으로만 맞춘다. 그래서 "김철수로 주문하고 고길동이 입금" 같은 건이
  자동으로 안 붙는다. 이 항목이 그 근본 원인을 없앤다.
- **범위** `orders` 컬럼 추가(마이그레이션) + 체크아웃 화면 / `ehyun` + `ui`
- **완료조건** 주문 시 입금자명이 저장되고, 자동 대사가 그 값을 후보로 쓴다.
- **연관** 「[신규] 회원가입 시 정보 받기」(같은 값을 회원 정보로도 받으려는 것), 자동 대사 로직

### [신규] 회원가입 시 정보 받기
- **우선순위** 우선 · **상태** 확인 필요 · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541104841949192252
- **요구**
  1. 카카오 로그인에서 받을 수 있는지 **먼저 확인**
  2. 안 되면 가입 시 또는 첫 주문 시 폼으로
  3. 기존 사용자는 직접 입력 → **가입자 관리 페이지(수정·권한) 필요**
  - 이름 = 입금자명으로 간주 / 연락처 = 문제 생길 때 연락용
- **조사** 지금 카카오에 요청하는 동의항목은 `profile_nickname`, `profile_image`
  둘뿐이다(`server/src/kakao.ts`). 이름·전화번호는 **비즈니스 앱 전환 + 검수**가
  있어야 열린다. 즉 1번은 즉답이 아니라 신청 절차다.
- **범위** `profiles` 컬럼, 카카오 스코프, 관리자 화면 / `ehyun` + `ui`
- **미결** 비즈니스 앱 신청할 것인가, 아니면 폼으로 갈 것인가

### [신규] 매장 페이지 상품 할인 설정 기능
- **우선순위** 우선 · **상태** 대기 · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541103006727012363
- **대상** https://farmassi.kr/admin/farms/{farmId}/products
- **요구** 수정에서 원래가격·할인가격 설정 → 관리 페이지와 주문 페이지에서
  기존가격에 취소선, 할인가로 표시
- **요청자 메모** "AI한테 맡겨서 느낌대로 일단 구현하면 내가 나중에 수정해봄"
- **조사** `products` 에 할인 관련 컬럼이 없다(가격은 `price` 하나). 컬럼 추가 필요.
- **범위** `products` 마이그레이션 + 상품 관리·매장 화면 / `ehyun` + `ui`

### [수정] 주문 상태 수정
- **우선순위** 우선 · **상태** 대기 · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541107567093350421
- **요구**

  | 현재 | 변경 |
  |---|---|
  | 포장중 | **송장 발급 완료** |
  | 완료 | **배송완료** |

  (입금대기 · 입금완료 · 배송중 · 취소는 그대로)
- **조사** 표시 문구만 바꾸면 된다. `src/lib/orderStatus.ts:3` 의 `statusLabels`
  (`packing: '포장중'`, `completed: '완료'`).
  **DB 의 status 값은 건드리지 않는다** — 값을 바꾸면 기존 주문과 RLS·조회가 깨진다.
- **범위** `src/lib/orderStatus.ts` / `ui`
- **완료조건** 관리자·농가·고객 화면 전부에서 새 문구로 보인다. DB 값 불변.

### [수정] 상품 수정 페이지 항목 수정
- **우선순위** 우선 · **상태** 대기 · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541109818373443684

**요구 1 — 판매상태에 「별도 문의」 추가**

지금은 `on_sale / coming_soon / sold_out / hidden` 네 가지다
(`src/types/models.ts:14`, DB `products_sale_status_check`).
값을 하나 늘리는 것이므로 **프론트 타입 + DB 체크 제약 양쪽**을 고쳐야 한다.
주문 가능 여부(`isProductOrderable`)에서 어떻게 다룰지도 정해야 한다 —
「별도 문의」는 장바구니에 담기면 안 될 가능성이 높다.

**요구 2 — 택배 중량·부피를 우체국 요금 구간에 맞추기** ("둘 다 부탁")

여기가 처음 생각과 다르다. **컬럼은 이미 있다** —
`products.parcel_weight_kg`, `parcel_volume_cm` (둘 다 text).
그리고 이 값은 화면 표시용이 아니라 **우체국 창구소포 파일접수양식 엑셀**에
그대로 들어간다 (`src/lib/kpostParcelExcel.ts`, 시트명 `창구소포 파일접수양식`).
따라서 값을 마음대로 바꾸면 접수 파일이 거부될 수 있다.

현재 허용값과 요금표를 맞춰 보면:

| | 현재 허용값 | 요금표 구간(상한) | 차이 |
|---|---|---|---|
| 부피 | 80 / 100 / 120 / 160 | 80 / 100 / 120 / 160 | **이미 일치** |
| 중량 | 3 / 5 / 10 / 20 / 30 | 3 / 5 / **7** / 10 / **15** / 20 / **25** / 30 | **7·15·25 빠짐** |

즉 값 자체는 구간의 **상한**이고, 부피는 이미 맞다. 실제 문제는 둘이다.

1. **중량 구간 3개(7·15·25kg)가 빠져 있다.** 요금이 달라지는 구간인데 고를 수 없다.
2. **화면에 상한값만 보여서 뜻이 모호하다.** `5kg` 이 "정확히 5kg" 인지
   "3kg 초과 5kg 이하" 인지 알 수 없다. 요청자가 요금표 사진을 올리며
   "저 무한 표시가 ~일거임" 이라고 한 것이 이 지점이다.
   → 값은 그대로 두고 **라벨만 `3kg 이하` · `3~5kg` · `5~7kg` …** 로 바꾸면
   엑셀 양식을 건드리지 않고 해결된다.

요금(등기소포 창구접수 익일배달): 4,000 / 4,500 / 5,000 / 6,000 / 7,000 /
8,000 / 11,000 / 13,000원. **제주는 별도** — 제주→육지 익일배달은 6,500~15,500원이고
제주 익일배달 요금은 제주발에만 적용된다.
출처: https://parcel.epost.go.kr/parcel/use_guide/charge_1.jsp

- **범위** `src/lib/kpostParcelExcel.ts`(허용값·라벨), 상품 수정 화면,
  DB 체크 제약 `products_parcel_weight_kg_check`, `products_sale_status_check` / `ehyun` + `ui`
- **완료조건** 중량 8구간·부피 4구간을 범위 라벨로 고를 수 있고,
  생성한 우체국 엑셀이 그대로 접수된다.
- **미결**
  - 우체국 양식이 중량 `7 / 15 / 25` 를 받아주는가 (**확인 전에는 추가하지 말 것**)
  - 요금을 화면에 계산해 보여줄 것인가, 구간만 고르게 할 것인가
  - 「별도 문의」 상품을 주문 가능으로 볼 것인가

### [신규] 농가별 배송가능 요일 추가
- **우선순위** 우선 · **상태** 대기 · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541098558688600064
- **요구**
  - `farms` 에 배송가능 요일 추가
  - 농가 추가·수정 페이지에서 **요일 체크박스**로 설정
  - 주문 페이지에서 그 요일에 따라 **예상 배송일** 표시
- **디자인** https://www.figma.com/design/giJRKuOIr7VLnXUxMkNWpy/제목-없음?node-id=1-3
  (농가 추가 `#1896005124`, 수정 `#1896002057`)
- **참고 화면** https://dev.farmassi.kr/farm/haneul-farm
- **범위** 마이그레이션 + 농가 관리 화면 + 주문 화면 / `ehyun` + `ui`
- **연관** 「[신규] 배송 일시정지 기능 필요」 — 요일이 아니라 **기간**으로 막는 별개 기능

---

## 일반

### [신규] 배송 일시정지 기능 필요
- **우선순위** 일반 · **기한 추석 이전 주까지** · **상태** 대기 · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541116434812240002
- **요구** 농가 관리에서 **기간을 지정해** 배송 정지. 추석 연휴 등에 쓴다.
- **요청자 강조** 「[신규] 농가별 배송가능 요일 추가」은 요일별이고, 이건 **기간 지정으로 따로 필요하다**
- **범위** 마이그레이션 + 농가 관리 화면 / `ehyun` + `ui`

### [신규] 매장 페이지 상품 기본 폼 불러오기 기능
- **우선순위** 일반 · **상태** 대기 · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541104302540587048
- **요구** "새 상품 추가" 버튼 옆에 **불러오기** — 다른 상품을 가져와 작성
- **요청자 강조** "불러오면 당연히 기존 상품은 **절대 수정되지 않고** 복사본 만들어서"
- **범위** 상품 관리 화면 / `ui`

### [수정] 더미 삭제 ⚠️
- **우선순위** 일반 · **상태** 대기 · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541099952405487727
- **요구**
  - 운영에서 **하늘농원, 바람들녘 삭제**
  - 주문·입금도 "윤현우님 빼고 싹 지우기"
  - **dev 더미는 그대로 둔다**
- **절대 지우면 안 되는 것** — 요청자가 못박음
  ```
  윤현우 / FA20260823-LXHC / 진영농원 / 45,000원 / 캠벨 5kg ×1
  ```
- **조사** 하늘농원·바람들녘은 **뱅크다에 계좌가 등록된 유일한 두 농가**다.
  지우면 `private.bankda_merchant` 와 스크래핑 상태도 함께 정리해야 하고,
  뱅크다 쪽 가맹점은 우리 DB 를 지운다고 사라지지 않는다.
- **범위** 운영 DB / `ehyun`
- **미결** 뱅크다 가맹점·계좌 등록도 해제할 것인가
- **주의** 되돌릴 수 없다. 실행 전 백업 확인하고, 지울 목록을 먼저 보여주고 승인받을 것.

---

## 확인 필요

### dev 서버 뱅크다 환경변수 (수정방 밖)
- **상태** 의도적으로 비워 둠 (변경하려면 결정 필요)
- **사실** `server/.env.dev` 에서 `BANKDA_ACCESS_TOKEN`, `SMTP_PASSWORD` 가 비어 있고
  `SCRAPE_CHECK_MINUTES=0` 이다.
- **비워 둔 이유**
  1. 뱅크다 계정이 하나뿐이다. dev 에서 토큰을 쓰면 실제 가맹점·계좌가 만들어진다.
  2. 거래내역 조회 5분 제한을 운영과 나눠 쓰게 된다.
  3. **dev DB 는 개인정보를 마스킹한 복사본인데, 뱅크다 조회는 실제 입금자 실명을
     가져온다.** 게다가 dev 계좌번호가 가짜라 어느 농가와도 안 맞아
     `farm_id = null` 로 실명만 쌓인다. 마스킹한 의미가 사라진다.
- **선택지**
  - A. 그대로 (지금)
  - B. 토큰만 넣고 스케줄러는 끈 채로 — 계좌 상태 조회는 가능, 자동 수집 없음.
    단 OTT 발급 버튼은 누르면 **실제** 가맹점이 생긴다.
  - C. 전부 켜기 — 3번 때문에 권하지 않음
- **미결** A / B / C

---

## 미결 질문

진행하려면 답이 필요한 것들. 답을 받으면 해당 항목의 **미결** 에서 지우고 본문에 반영한다.

| 항목 | 물어볼 것 | 막히는 정도 |
|---|---|---|
| **[신규] 주문 들어오면 알림 필요** | 알림을 무엇으로 보낼까 (메일 / 디스코드 봇 / 푸시 구독). 메일이면 주소 | **못 시작** |
| **[수정] 상품 수정 페이지 항목 수정** | 우체국 양식이 중량 7 / 15 / 25kg 을 받는가 | 일부 |
| **[수정] 상품 수정 페이지 항목 수정** | 요금을 계산해 보여줄 것인가, 구간만 고르게 할 것인가 | 일부 |
| **[수정] 상품 수정 페이지 항목 수정** | 「별도 문의」 상품을 주문 가능으로 볼 것인가 | 일부 |
| **[신규] 회원가입 시 정보 받기** | 카카오 비즈니스 앱 신청할 것인가, 폼으로 받을 것인가 | 방향 갈림 |
| **[수정] 더미 삭제** | 뱅크다 가맹점·계좌 등록도 해제할 것인가 | 일부 |
| **dev 서버 뱅크다 환경변수** | 토큰을 넣을 것인가 (그대로 / 토큰만 / 전부) | 결정 |

---

## 우리 쪽에서 나온 것 (수정방 밖)

요청받은 건 아니지만 하기로 했거나 해둬야 하는 일. 같은 규칙으로 관리한다.

### 파이썬(FastAPI) 이관
- **상태** 코드 완료, 전환 남음 · 대조 102건 일치
- 남은 것: launchd 등록 → nginx 경로 전환 → 스케줄러 이관(동시 실행 금지) → Node 정리
- **계획** dev(`dev.api.farmassi.kr`)를 먼저 파이썬으로 통째로 바꿔 며칠 굴려보고 운영 전환

### DB 접속 격리
- **상태** 대기 · 사용자 확인함
- `datacl` 이 PUBLIC 에 CONNECT 를 열어 둬서 `farmassi_dev_*` 롤로 **운영 DB 가 읽힌다**
  (실제로 실명이 조회됐다). 지금 분리는 접속 문자열 한 줄에만 의존한다.
- 조치: 각 DB 에서 PUBLIC 의 CONNECT 회수 후 짝 롤에만 부여.
  `ops_ro` 는 양쪽 필요, jpark 계열은 운영만.

### develop → main 머지
- **상태** 대기 · `main` 미반영 커밋 20건+
- 버전 배지, 입금 원장·수동 대사, 비활성 농가 열람 허용, 수령인 연락처 표시,
  robots.txt 등이 아직 운영에 없다.

### 노출된 시크릿 교체
- Gmail 앱 비밀번호(대화창 노출), 네이버 Client Secret(공개 번들에 잠시 포함)
- 네이버 키는 **지도 SDK 전용**이다. 서버 지오코딩은 카카오라 무관하다.

### 지도 카카오맵 전환
- **상태** 착수 전 (조사 완료)
- `src/lib/naverMap.ts` 의 SDK·렌더 부분만 교체하면 되고, 지오코딩은 이미 카카오다.
- 끝나면 네이버 의존이 완전히 사라진다. 카카오 콘솔에 Web 플랫폼 도메인 등록 필요.

### 외부 콘솔 등록 (사용자 작업)
- 카카오: 리다이렉트 URI `https://dev.api.farmassi.kr/auth/kakao/callback`
  → **없으면 dev 로그인이 안 된다**
- 카카오 Web 플랫폼: `farmassi.kr`, `www.farmassi.kr`, `dev.farmassi.kr`, `http://localhost:5173`

### 백업 사본 외부 보관
- 지금 백업이 이 맥에만 있다. 기기가 죽으면 운영 DB 와 백업이 함께 사라진다.

---

## 완료

### [버그] dev.farmassi.kr/farms 접속시 사진 문제발생 ☑️
- **상태** ☑️ 처리됨(반응) · **요청** bagjihun8116
- **스레드** https://discord.com/channels/1458046783795236947/1541099087171166309
- **요구** "dev.farmassi.kr/farms 접속시 사진 문제발생" (본문은 이 한 줄 + 스크린샷)
- **조사 (2026-08-24)**
  - 스크린샷은 `/farms` 가 아니라 **`/admin/farms`** 화면이다.
  - 거기 눈에 띄는 건 사진이 아니라 검은 토스트
    **"BANKDA_ACCESS_TOKEN 이 설정되지 않았습니다."** → 이건 「dev 서버 뱅크다 환경변수」 건이다.
  - dev 상품 이미지 URL 15개를 전부 요청해 봤다. **모두 HTTP 200.**
  - dev 업로드 디렉터리 파일 22개로 운영과 같다.
    ("dev 에 파일을 안 복사해서 깨진 것" 이라는 추측은 **틀렸다**.)
- **결론** 요청자가 채널 본문에 ☑️ 를 달았다. 물어볼 것 없음.

### 주문 카드에 수령인 연락처 표시 (2026-08-23)
`recipient_phone` 을 DB 에서 가져오면서도 `OrderListModel` 에 필드가 없어
뷰 모델 변환 단계에서 버려지고 있었다. `tel:` 링크로 표시. 커밋 `27c8b38`.
아직 `develop` 까지만 올라가 있다 → 「develop → main 머지」 참고.
